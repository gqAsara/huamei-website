/**
 * Bare layout for /studio — no marketing chrome, no fonts, no global CSS.
 * Sanity Studio brings its own styles.
 */
export default function StudioLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
